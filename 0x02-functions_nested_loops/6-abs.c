#include "main.h"

/**
 * _abs - returns the absolute value of an integer
 * @n: the number to convert
 * Return: returns 0
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
