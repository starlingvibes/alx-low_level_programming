#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: the string to check
 * Return: returns the length of the string
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
