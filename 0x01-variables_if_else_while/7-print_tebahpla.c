#include<stdio.h>

/**
 *	main - prints all lowercase letters in reverse
 *	Return: returns 0 (success)
 */
int main(void)
{
	char ch;

	for (ch = 'z'; ch >= 'a'; ch--)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
